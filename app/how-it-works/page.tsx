import { ArrowLink, Eyebrow, Footer, Header, MotionVisual, QuoteBand } from '@/components/site-shell'

const processSteps = [['01', 'Signal fires', 'Strategy, TradingView alert, or AI assistant hits our backend webhook.'], ['02', 'Risk engine decides', 'Kill-switch, stop-loss, sizing, and profit-booking are checked and logged.'], ['03', 'Approved order', 'The order goes to the broker regulated system.'], ['04', 'Broker executes', 'Their server executes on the exchange. Their infra, their liability.'], ['05', 'Result logged', 'The trade and its reasoning are pushed to the client.']] as const

export default function HowItWorksPage() { return <main className="how-page"><Header /><section className="how-hero"><div className="site-shell"><Eyebrow>HOW IT WORKS / 00—05</Eyebrow><h1>A clear chain of custody.<br /><em>Nothing happens in the dark.</em></h1><p>From signal to result, every handoff is visible, rule-checked, and logged.</p><MotionVisual /></div></section><section className="process-section"><div className="site-shell process-list">{processSteps.map(([number, title, body], index) => <div className="process-row" key={number}><span className="process-number">{number}</span><div className={`process-visual pv-${index}`}><span>{index === 0 ? '↗' : index === 1 ? '◇' : index === 2 ? '→' : index === 3 ? '◉' : '✓'}</span></div><div><Eyebrow>{title}</Eyebrow><h2>{title}</h2><p>{body}</p></div></div>)}</div></section><section className="how-close"><div className="site-shell"><h2>The system is strict<br /><em>so you don&apos;t have to be.</em></h2><ArrowLink href="/our-promise">See what we stand for</ArrowLink></div></section><QuoteBand /><Footer /></main> }
